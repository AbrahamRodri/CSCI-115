#include <iostream>
using namespace std;
#include <cmath>

void f ( int* a , int n){
    cout << "size" << n << endl;
    cout << "hello" << endl;

    for(int i = 0; i / 2 ; i++){      // n/2
        a[i] = 0;       
    }

    for(int i = 0; i / 2 ; i++){      // n/2
        a[i] = 0;       
    }

    //f(n) = 2 + 2 * (n/2)
}

/*     Exercise: 

    Show that 2^n = O(n!)    2^n/n! will aproach 0 as it goes to infinity 

              f(n)  g(n)     so 2^n = O(n!) since f(n) / g(n) != inf and does approach 0


*/ 



int main () {

    return 0;
}